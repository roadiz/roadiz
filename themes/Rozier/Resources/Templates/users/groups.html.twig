{% extends 'base.html.twig' %}

{% block content %}
<section id="manage-user-groups">
	<h1 id="user-title">Edit {{ user.getDefaultTranslatedTag.getName }} groups</h1>
	
	{% include 'users/navBar.html.twig' with {'user':user, current:'groups'} %}

	<table class="users-groups uk-table">
		<thead>
	        <tr>
	            <th>{% trans %}Group{% endtrans %}</th>
	            <th>{% trans %}Actions{% endtrans %}</th>
	        </tr>
	    </thead>
		<tbody>
			{% for group in user.getGroups %}
			<tr>
				<td>{{ group.getName }}</td>
				<td>
					<a class="uk-button uk-button-small uk-button-danger" href="{#{{ path('usersRemoveRolesPage', { user_id: user.getId, role_id: role.getId }) }}#}">{% trans %}Unlink{% endtrans %}</a>
				</td>
			</tr>
			{% endfor %}
		</tbody>
	</table>
	<h2>{% trans %}Add a group{% endtrans %}</h2>
	<form class="uk-form uk-form-stacked" action="#" method="post" {{ form_enctype(form) }}>
   		{{ form_errors(form) }}

   		<fieldset data-uk-margin>
		    {% for item in form %}
			    {% if item.vars.name != "_token" and item.vars.name != 'user_id' %}
			    <div class="uk-form-row">
			    	{# Source fields  #}
			        <label class="uk-form-label"for="{{ item.vars.id }}">{% if item.vars.label %}{{ item.vars.label|trans }}{% else %}{{ item.vars.name|trans }}{% endif %}</label>
			        {{ form_errors(item) }}
			        {{ form_widget(item) }}
			    </div>
			    {% endif %}
		    {% endfor %}
   		</fieldset>
	    {{ form_rest(form) }}
	    <fieldset data-uk-margin>
	    	<input class="uk-button uk-button-primary" type="submit" />
   		</fieldset>
	</form>
</section>
{% endblock %}