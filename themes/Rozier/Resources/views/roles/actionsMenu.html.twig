{% spaceless %}
<nav data-action-menu class="actions-menu uk-navbar rz-vertical-align">
    {# Delete current node #}
    {% if (securityContext.Granted('ROLE_ACCESS_ROLES')) %}
        {% if role.getId %}
        <a class="uk-button uk-button-danger" href="{{ path('rolesDeletePage', { roleId: role.getId}) }}">
            <span class="icon-container"><i class="uk-icon-rz-trash-o"></i></span>
            <span class="label"><span class="label-text">{% trans %}delete.role{% endtrans %}</span></span>
        </a>
        {% endif %}
    {% endif %}
</nav>
{% endspaceless %}
