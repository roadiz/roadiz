{% apply spaceless %}
<nav data-action-menu class="actions-menu uk-navbar">
    {# Delete current node #}
    {% if (is_granted('ROLE_ACCESS_CUSTOMFORMS_DELETE')) %}
    {% if field.id %}
    <a class="uk-button uk-button-danger" href="{{ path('customFormFieldsDeletePage', { customFormFieldId: field.id}) }}">
        <span class="icon-container"><i class="uk-icon-rz-trash-o"></i></span>
        <span class="label"><span class="label-text">{% trans %}delete.customFormField{% endtrans %}</span></span>
    </a>
    {% endif %}
    {% endif %}
</nav>
{% endapply %}
