{% extends '@Rozier/layout.html.twig' %}

{% block content %}
<section class="content-global edit-item">

    <header class="content-header header-item header-item-add">
        <h1 class="content-title item-add-title">{{ "edit.attribute.%name%"|trans({'%name%': item.name}) }}</h1>
        <a class="content-header-nav-back uk-navbar-content" href="{{ path('attributeGroupsHomePage') }}" title="{% trans %}back_to.attribute_groups{% endtrans %}" data-uk-tooltip="{animation:true}"><i class="uk-icon-rz-back-parent"></i></a>
    </header>

    <article class="content content-edit">
        {% form_theme form '@Rozier/forms.html.twig' %}
        {{ form_start(form, { attr: { id: "edit-attribute-form" }}) }}
            {{ form_widget(form) }}
            <fieldset data-uk-margin>
                <button data-action-save="#edit-attribute-form" class="uk-button uk-button-primary rz-action-save" type="submit">
                    <span class="icon-container"><i class="uk-icon-rz-save-mini"></i></span>
                    <span class="label"><span class="label-text">{% trans %}save{% endtrans %}</span></span>
                </button>
            </fieldset>
        {{ form_end(form) }}
    </article>

    {% include '@Rozier/attributes/groups/actionsMenu.html.twig' with {"item": item} only %}
</section>
{% endblock %}
