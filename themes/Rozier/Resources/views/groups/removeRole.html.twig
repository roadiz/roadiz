{% extends 'base.html.twig' %}


{% block content %}
<section class="content-global unlink-role-group">
	<header class="content-header header-group header-group-edit">
        <h1 class="content-title group-add-title">{{ "unlink.role.%name%"|trans({'%name%': role.getName}) }}</h1>
    </header>

    <article class="content content-delete content-no-action-menu">
		{% form_theme form 'forms.html.twig' %}
		<form class="uk-form uk-form-stacked" action="#" method="post" {{ form_enctype(form) }}>
		    {{ form_widget(form) }}
		    <fieldset data-uk-margin>
		    	<legend>{% trans %}are_you_sure.remove.role.from.group{% endtrans %}</legend>
		    	<a href="{{ path('groupsEditRolesPage', { groupId: group.getId }) }}" class="uk-button"><i class="uk-icon-reply"></i> {% trans %}cancel{% endtrans %}</a>
		    	<button class="uk-button uk-button-danger" type="submit"><i class="uk-icon-trash-o"></i> {% trans %}unlink{% endtrans %}</button>
	   		</fieldset>
		</form>
	</article>
</section>
{% endblock %}
