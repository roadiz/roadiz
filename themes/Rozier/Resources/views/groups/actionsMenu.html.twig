<nav data-action-menu class="actions-menu uk-navbar rz-vertical-align">
    {% spaceless %}
    {# Delete current node #}
    {% if (securityContext.Granted('ROLE_ACCESS_GROUPS')) %}
        {% if group.getId %}
        <a class="uk-button uk-button-danger" href="{{ path('groupsDeletePage', { groupId: group.getId}) }}">
            <span class="icon-container"><i class="uk-icon-trash-o"></i></span>
            <span class="label"><span class="label-text">{% trans %}delete{% endtrans %}</span></span>
        </a>
        {% endif %}
    {% endif %}
    {% endspaceless %}
</nav>
