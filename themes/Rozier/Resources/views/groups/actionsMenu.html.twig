<nav data-action-menu class="actions-menu uk-navbar">
    {% apply spaceless %}
    {# Delete current node #}
    {% if (is_granted('ROLE_ACCESS_GROUPS')) and is_granted(item) %}
        {% if item.id %}
            <a class="uk-button uk-button-danger" href="{{ path('groupsDeletePage', { id: item.getId}) }}">
                <span class="icon-container"><i class="uk-icon-trash-o"></i></span>
                <span class="label"><span class="label-text">{% trans %}delete{% endtrans %}</span></span>
            </a>
        {% endif %}
    {% endif %}
    {% endapply %}
</nav>
