{% extends 'base.html.twig' %}

{% block content %}
<section id="manage-setting">
	<h1 id="setting-title">{% trans %}Edit{% endtrans %} {{ setting.getDisplayName }}</h1>
	<nav><a class="uk-button uk-button-small" href="{{ path('settingsHomePage') }}"><i class="uk-icon-reply"></i> {% trans %}Back to settings{% endtrans %}</a></nav>

	<form class="uk-form uk-form-stacked" action="#" method="post" {{ form_enctype(form) }}>
   		{{ form_errors(form) }}

   		<fieldset data-uk-margin>
		    {% for item in form %}
		    	{# Node fields  #}
		        {% if item.vars.name != "_token" and item.vars.name != "id" %}
		    	<div class="uk-form-row">
		        	<label class="uk-form-label"for="{{ item.vars.id }}">{% if item.vars.label %}{{ item.vars.label|trans }}{% else %}{{ item.vars.name|trans }}{% endif %}</label>
			        {{ form_errors(item) }}
			        {{ form_widget(item) }}
		    	</div>
		        {% endif %}
		    {% endfor %}
   		</fieldset>
	    {{ form_rest(form) }}
	    <fieldset data-uk-margin>
	    	<input class="uk-button uk-button-primary" type="submit" />
   		</fieldset>
	</form>
</section>
{% endblock %}