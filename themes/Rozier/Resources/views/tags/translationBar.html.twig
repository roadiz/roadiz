<nav class="tag-translation-bar translation-bar uk-navbar">
    {% spaceless %}
	<ul class="uk-navbar-nav">
		{% for translation in available_translations %}
		    <li class="{% if current == translation.getId %}uk-active{% endif %}">
		    	<a href="{{ path('tagsEditTranslatedPage', { tagId: tag.getId, translationId:translation.getId }) }}">{{ translation.getShortLocale }}</a>
		    </li>
		{% endfor %}
	</ul>
    {% endspaceless %}
</nav>
