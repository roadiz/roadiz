{% if session.user.getFirstName %}
{% set displayName = session.user.getFirstName %}
{% else %}
{% set displayName = session.user.getUsername %}
{% endif %}

{%  spaceless %}
<div>
    <div id="company">
        {% if head.companyLogo %}
        {% else %}
            <a class="uk-icon-rz-rezozero" href="{{ path('adminHomePage') }}"></a>
        {% endif %}
    </div>
	<div id="user">
        <a id="user-picture" href="{{ path('usersEditPage', {'userId':session.user.getId }) }}"><img width="200" height="200" class="uk-thumbnail" src="{{ session.user.getPictureUrl }}" alt="{{ session.user.getUsername }}" /></a>
        <h3>{{ "hello.%name%"|trans({'%name%': displayName}) }}</h3>
        <nav class="user-actions">
            {# <a title="{% trans %}edit.account{% endtrans %}" class="uk-icon-button uk-icon-user" href="{{ path('usersEditPage', {'userId':session.user.getId }) }}"></a> #}
            {% if (securityContext.Granted('ROLE_ACCESS_DOCTRINE_CACHE_DELETE')) %}
            <a title="{% trans %}delete.caches{% endtrans %}" class="uk-icon-button uk-icon-rz-flash" href="{{ path('deleteDoctrineCache') }}"></a>
            {% endif %}
            {% if (securityContext.Granted('ROLE_ACCESS_DOCTRINE_CACHE_DELETE')) %}
            <a title="{% trans %}delete.caches{% endtrans %}" class="uk-icon-button uk-icon-rz-flashimage" href="{{ path('deleteDoctrineCache') }}"></a>
            {% endif %}
            <a title="{% trans %}logout{% endtrans %}" class="uk-icon-button uk-icon-rz-turn-off" href="{{ path('logoutPage') }}"></a>
            {% if head.devMode %}
            <p class="dev-mode"><i class="uk-icon-rz-cogs"></i> {% trans %}dev_mode.activated{% endtrans %}</p>
            {% endif %}
        </nav>

    </div>
    <footer id="cms-version">
        <a target="_blank" title="{% trans %}help{% endtrans %}" class="uk-icon-button uk-icon-rz-question-mark" href="http://doc.rezo-zero.com"></a>
        <p class="cms-version"><i class="uk-icon-rz-rezozero"></i></p>
        <p>{{ head.cmsVersion }} - build {{ head.cmsBuild }}</p>
    </footer>
</div>
 {%  endspaceless %}
