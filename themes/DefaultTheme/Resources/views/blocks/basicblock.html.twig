<article class="uk-width-medium-1-3 basic-block" data-index="{{ loop.index }}">
    <p>{{ loop.index }}. {{ nodeSource.title }}</p>
    <div class="block-content">{{ nodeSource.content|markdown }}</div>
</article>